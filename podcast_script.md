# 英伟达杀手？谷歌芯片抢走百亿订单，老黄慌了吗？

我们所说的这一段时间在发舞，我们说要朝着比较好的方向走，我们说我刚才提到的日子确实不错，但之后很多人说、很多人都在说，这也是很多人正在做的事情，所以我们体会到需要整个人都去到那边。我们讲的时间，我刚才说的“镭面”，到底是什么问题？哦哦哦，对我们人也变得像了，所以我们讲的“镭面”，那么，“日子镭面”这些东西都成了人的账困，什么是好的呢，所以，混合专家（Mixture of Experts）就是……

现在你所知的很多最现代的高性能系统都是这样构建和部署的。曾有过有趣的一周，视频中 GPT-4 实际上可能被揭示为 GPT MoE-1.8T，但更广泛地说，其他像 GROK、DeepSeek 和 Llama 4 现在都已采用混合专家架构。到了 2025 年这个节点，混合专家相对于稠密架构的优势已经非常清晰。基本上在所有计算规模下，只要训练得当，混合专家模型都会比稠密模型带来收益，因此东西方似乎都在这样做。如果你正试图在你拥有的算力（FLOPs）预算内构建出最好的模型，理解这一点就很重要。

混合专家其实非常简单，只是名字起得特别糟糕。你听到“混合专家”，可能会想：“哦，那一定是有不同领域的专家，他们各自负责不同的事，比如有一个编程专家、一个英语专家、一个其他语言的专家。” 这与实际的心理模型相去甚远。混合专家是一种精巧的架构，它包含多个子组件，称为“专家”，并以稀疏的方式激活。特别要指出的是，当思考混合专家时，你应该把注意力放在多层感知机（MLP）上，这才是所有关键所在。所以，一个 MoE 架构和一个非 MoE 架构，在几乎所有组件上都是相似的，只有一个例外。如果你看这边的幻灯片，这是一个标准 Transformer 的组件，你有自注意力，你有前馈网络（FFN）。放大来看，在稠密模型中，前馈组件只是一个很大的块。而在稀疏模型中，你会把这个 FFN 分割开或者复制多份，具体取决于你如何设置你的 MoE。你会拥有多个 FFN（即全连接网络）的副本，并设置一个路由器（router），在每次前向传播（即每次推理）中，选择其中一小部分来使用。这就是 MoE 背后的基本思想。我们将左侧那个大块的前馈网络，替换为一个选择器层和多个更小的前馈网络。这样做有什么好处呢？如果它是稀疏激活的，比如每次只选一个专家，并且该专家的大小与你的稠密 FFN 相同，那么左边和右边，稠密模型和 MoE 模型，它们的 FLOPs（浮点运算次数）是一样的。你在前向传播中做的矩阵乘法次数相同。因此，你拥有了更多参数，却没有影响 FLOPs。如果你相信重要的是拥有更多参数（比如用于记忆世界知识），那么这就是一个很棒的架构。这样你应该就能直观理解 MoE 背后的思想了，希望这些都讲清楚了。

你可能想知道，好吧，每个 FLOP 拥有更多参数说得通，但这真的能转化为训练出来的模型有更好的性能吗？我认为，目前已有非常非常多的论文表明，在相同的 FLOPs 总数下，也就是使用相同的训练算力，混合专家模型的性能要优于稠密模型。这是一篇不错的论文。今天我会回顾几篇奠定这一领域的经典 Google 论文，其中一篇就是费杜斯等人（Fedus et al.）在 2022 年发表的。他们展示的结果是，如果将训练 FLOPs 匹配好，也就是训练使用的总计算量相同，那么随着专家数量的增加，语言模型的训练损失会持续下降、再下降。所以，专家越多越好。当然，专家并非没有成本，你需要存储这些专家的内存，当你进行并行化时，你必须考虑如何将数据路由到 256 个独立的专家上，这会带来系统复杂性。但如果只考虑 FLOPs，这张图很有说服力，因为你用了相同的 FLOPs，但测试损失却降低了。右侧的图也反映了同样的情况：随着训练时间拉长，拥有 128 个专家的 Switch Transformer 模型，也就是专家更多的模型，其困惑度下降得更快。希望这些已经相当清楚。你可能会说，这是 2022 年的论文，在现代架构、现代规模下这还成立吗？它依然非常成立。AI2 有一篇关于 Olmo 的非常好的论文，他们在各种设置下对稠密架构、MoE 和其他架构进行了仔细控制的比较，结果完全一致。左边这张还是费杜斯等人论文的图，展示了多专家带来的 7 倍加速；右边是 Olmo 的比较，粉色线是 MoE，T5-v1.1 是稠密架构，可以看到稠密模型的训练损失下降速度比 MoE 慢得多。所以，希望我已经以某种方式让你相信了 MoE 的价值，值得学习这种稍微新一点的架构。我们要为此付出一定代价，但至少从 FLOPs 层面来看，这非常吸引人。

有提问。刚才的问题是，在上一节课我曾说，即使是很小的非 FLOPs 开销，在墙钟时间上也可能非常大，那么在 MoE 领域是否会出现类似的情况？我认为 MoE 的缺点之一，也是为什么它不是在 224N 这类课程中被教授的标准内容，是因为要使这东西高效运行，会面临显著的工程复杂性。我会讲到这方面的。实际上是可以让这些架构非常高效的，尤其是当每个专家都驻留在不同的设备上时，这样你只需要将数据路由到不同地方，就能实现高效计算。但这并不容易，涉及大量基础设施问题，你会看到为了让这个架构跑起来，需要处理许多复杂性。但当它真的跑起来时，你就把所有的 FLOPs 都充分利用了。

我最后想展示的一点是，很多公司非常喜欢 MoE，因为可以得到看起来非常引人注目的图表，就像这张。这张图来自 DeepSeek V2 论文。X 轴这里有一点花招，它标注的是“仅激活参数”，也就是实际用于计算的参数，忽略了所有未激活的专家。Y 轴是 MMLU 性能。我们看到 DeepSeek V2，哇，非常少的激活参数，却获得了非常好的 MMLU 性能。如果你只关心训练和推理的 FLOPs，那么激活参数就是关键所在，它能带来极好的性能。而且这不仅仅是一项消融研究，这是一个真实的、花费了大量金钱训练并已部署上线的系统。我们也会在其他例子中看到这种模式重复出现。

还有一个系统层面上的好处，就是 MoE 让我们有了另一个并行化的维度。在系统相关的课程中，我会非常详细地讲解并行化，也就是如何将模型切分成许多小块，分布到众多不同的设备上。现在我只会做一个高层次的说明。当你有专家时，在专家层面进行并行化是一种非常自然的方式。你拥有多个不同的前馈块，可以把每个专家放在不同的设备上。而且因为专家是稀疏激活的，你只需要把你的词元（token）路由到合适的设备，计算就会在那个设备上进行。因此，专家层面形成了一个天然的分割点，可以将模型分片到不同设备上，这被称为“专家并行”（Expert Parallelism）。这也是 MoE 如此流行的另一个原因。如果你真的想并行化非常大的模型，那么这是一件你不得不做的事。有趣的是，我认为 MoE 最初在 Google 和许多前沿的闭源实验室中发展起来，但公开的成果实际上很多最早来自中国。去年，当 DeepSeek 等机构在做大量 MoE 工作时，成果很频繁地出现。而西方的开源团队，只是最近才开始做更多的 MoE 工作。比如 Mixtral，Grok（不过 Grok 并不开源），还有现在的 Llama，也已经采用了 MoE 架构。你看，Llama 4 刚刚发布，最新最强的模型，它也是一个稀疏 MoE。我会在讲解过程中也提到 Llama 4。

正如之前所说，一些中国团队的工作是这一领域的起点之一，如 Qwen 和 DeepSeek，他们在基准测试、理解和评估这些 MoE 成果方面做了一些非常出色的工作。Qwen 1.5 是我所知道的第一个拥有这种大规模、经过充分测试和记录完善的 MoE 模型，他们所做的事情是，取一个 Qwen 1.5 的稠密模型，用一个巧妙的技巧将其“升级改造”（upcycle）为混合专家模型。这是一种将稠密模型转化为 MoE 的聪明方法。相比于他们大约 7B 的稠密模型，他们在计算效率上展示了显著的提升，同时减少了总参数数量。而 DeepSeek，如今已非常有名，但在这些论文最初发表时并没有那么出名。他们在开源世界中做了我认为是真正奠基性的 MoE 工作。本次讲座的很大一部分内容，实际上就是追溯 DeepSeek 的 MoE 架构发展轨迹。如果你去看他们最初的 DeepSeek MoE 论文，你会看到非常漂亮的比较，展示了用特定数量的 FLOPs 训练一个稠密模型会怎样，训练一个没有智能路由的非常朴素的 MoE 又会怎样，然后如果使用了一种称为 Switch 型的、更聪明的路由方式又会怎样。你会看到所有这些仔细控制的对比结果，从稠密到稀疏（即最左列到最右列），所有基准测试指标在固定的 FLOPs 数量下，都得到了一致的改善。这是非常一致的。

我想，大概现在几乎所有人都听说过 DeepSeek V3 了。从某种意义上说，它是这一系列工作的集大成者。但如果你一直在关注 MoE，并对神经网络和语言建模的这一分支感到兴奋，你其实在 V3 流行起来之前，很早就会知道 DeepSeek 了。我们会在这节课的最后看到，实际上 DeepSeek V3 在架构上与最早的 DeepSeek MoE 并没有太大区别。他们在训练那些规模小得多的 20 亿参数模型时，就已经把架构基本弄清楚了。他们真的只是完善了工程细节，就得到了非常出色的产品，也就是 V3 模型。

好了，我想我花了相当多的时间来极力宣传 MoE，它们确实值得宣传，非常优秀。但我觉得还有一个问题……

为什么混合专家模型没有更广泛地流行呢？为什么它没有成为自然语言处理和语言建模课程中的标准教学内容？主要是因为它们非常复杂，架构也很混乱。我希望在未来几年它们能变得更简洁，但目前看来，它们仍然相当棘手。其中一个难点在于，基础设施非常复杂，而 MOE 最大的优势实际上体现在多节点训练的场景中——当你不得不把模型拆分到不同设备上时，使用 MOE 才开始显得合理。将专家模块分片到不同模型上，是一种非常自然的做法。但在达到那个规模之前，MOE 的表现可能并不那么理想。一些早期谷歌的论文确实讨论过这种权衡，他们指出，当你面对必须拆分的大规模模型时，使用专家架构会展现出独特的优势。

另外还有一些很棘手的问题。如果你仔细想想，将令牌路由到哪个专家这一决策，本身是一个很难学习的任务。在深度学习中，我们很偏爱可微的目标函数，也就是那些平滑、可以求梯度的东西。但路由决策不是可微的，因为我们必须选定并提交给某个具体的专家。这就意味着，如果这样做，我们将面临一个非常困难的优化问题。而要让训练目标正常运行，要么需要依赖启发式方法，要么会导致训练不稳定。因此，我们必须精心设计这些组件才能让它们有效工作。以上就是通常你可能不想随意使用 MOE 的两个原因。

那么 MOE 到底长什么样呢？如同我在本堂课开始时提到的，经典的 MOE 可以这样理解：你拿出那些全连接层，也就是前馈网络（FFN），然后将它们拆分或者复制，并在这些层之间采用稀疏的路由决策。当然，你也可以用类似的思路，设计一个稀疏路由的注意力层，已经有一部分论文和开源项目采用了这种方法。但在主要的模型发布中，这种做法其实相当罕见。我记得看到有人在网上讨论，说这种方法实际上更不稳定，很难实现一致的训练效果。我虽然没看到相关的消融实验来证实这一点，但确实几乎没有多少人用 MOE 注意力机制来训练这类模型。

现在，我已经给你们介绍了基本架构，对吧？它其实非常简单。你有一个某种形式的路由器，根据路由结果，再通过不同的多层感知机（MLP）进行处理。那么，不同的 MOE 设计之间会有哪些变化呢？你可能会问：我们如何进行路由？路由函数的选择显然很重要。需要多少专家，每个专家规模应该多大？这是另一个选择。最后一个问题是，我们如何训练这个路由器？这个不可微的目标看起来很难训练。这些都是非常重要的设计问题，我们将逐一讲解，希望能覆盖到这些 MOE 相关设计空间的方方面面。在我深入讲每一个子组件之前，大家有什么问题吗？

好的。如果你只是希望对 2022 年前后的 MOE 有一个大致的了解，有一篇 2022 年由 Stas 等人撰写的调查报告或综述论文写得很好，涵盖了这里面的很多内容。我展示的许多图表都出自那篇论文。

当我们思考如何进行路由，或者说如何将令牌匹配给专家时，这正是 MOE 的核心。因为 MOE 做的事情就是：令牌不断涌入，你有一个正在处理的序列，这些序列将会被分配给各个专家，对吧？并非所有专家都会处理每一个令牌，这正是稀疏路由 MOE 的重点。那么你会问，这些路由决策是如何做出的呢？你大致有三种不同的选择。第一种是“令牌选择”，每个令牌对不同专家会有某种路由偏好，我会为每个令牌选择得分最高的前 K 个专家。第二种是“专家选择”，即每个专家会对令牌有自己的排序偏好，然后我为每个专家选择其最喜欢的 K 个令牌。这种方法有一个很好的好处，就是负载能自然地在专家间达到均衡。最后一种是通过解决某种复杂的优化问题，来确保专家和令牌之间的映射能以某种方式达到均衡，这属于“全局分配”。这里先给大家一点提示：几乎所有的 MOE 都采用了令牌选择加 Top-K 的方式。

在 MOE 的早期阶段，大家尝试过很多很多不同的方法，几乎覆盖了整个令牌路由器设计空间的方方面面。但如果你去观察那些主要的模型发布，它们基本上都收敛到了一类路由机制：令牌选择 Top-K。也就是说，每个令牌会根据与专家的亲和度对专家进行排序，然后为每个令牌选择得分最高的 K 个专家。OMO 模型（我在这堂课里会反复提到它，因为它有一系列很棒的消融实验，非常适合用来教学）恰好做过这样的对比。他们比较了令牌选择路由和专家选择路由，然后发现，从验证损失曲线上看，令牌选择的表现要好得多，损失下降得更快。

损失是关于隐藏状态的函数，对吧？令牌经过位置嵌入等处理后，隐藏状态会传递进来，然后由 MLP 进行处理…… 那对于另一种方式…… 专家选择令牌，你提到它能使专家负载更均衡，是指当前批次吗？处理的还是同一组令牌，但区别在于排序选择函数。在令牌选择中，我只是在列方向上取前 K 个，即使分数完全相同，我也是在列上取前 K。而在专家选择中，我是在行方向上取前 K。列方向取前 K 的好处是，你可以定义一种评分函数，比如评分代表每个令牌被每个专家处理得有多好，那么令牌选择会将该令牌路由给处理效果最好的专家，这对处理过程来说是有意义的。但专家选择的好处在于，每个专家恰好能获得相同数量的令牌，这样一来，如果你把不同的专家放在不同的设备上，就能实现均衡的利用率。所以，根据你路由时考虑的角度不同，存在不同的权衡。

问题：每个令牌怎么知道哪个专家是好的呢？这正是路由器的作用。我会给你们路由器的公式。但先给个小小的剧透，路由器比你们想的要轻量得多。假设你的令牌由一个向量 x 表示，也就是传入的隐藏状态（或残差流）。然后 x 会乘以一个矩阵 W，再经过一个类似 sigmoid 的函数，得到的就是它的分数。所以，它本质上就是一个向量与向量的内积，有点像注意力操作。

问题：这里的 K 是 1 吗？实际上，K 是一个超参数，不同的 MOE 会选择不同的值。我后面还会再谈到这个，但先给大家一个高层次的直观理解：最初 MOE 论文的论证是，K 应该大于 2，因为这样能获得一些探索的机会。如果 K 等于 1，你可能总是在利用当前的最优项，永远不知道其他选项的潜力。但如果 K 等于 2，这第二名就能提供一些探索信息。所以，K 等于 2 是经典的选择，直到现在也依然非常流行。这样的话，计算量会翻倍。因此，当人们谈论 MOE 时，通常会提到“激活参数数量”这样的概念，这已经考虑到了你将令牌送入多个专家处理的事实。

问题：当 K 等于 2 时，不同专家的输出如何组合？是的，或者当你…… 当 K 等于 1 时，输出的组合方式也是一样的。你看那边的注意力机制示意图，路由器将信号路由到上方的两个 MLP，然后它们的输出会被合并到一起，对吧？聚合方式是一样的。我已经介绍了一些常见的变体。实际上，从某些方面来说，要实现一个高性能的 MOE，你只需要了解 Top-K 路由就足够了。但我还是会讲一下其他变体，因为它们很自然会从你的思考中涌现出来。大多数 MOE 使用的是令牌选择的 Top-K 路由。

它是怎么工作的呢？你有残差流输入 X，它将进入一个路由器。如我所说，路由器非常像一个注意力操作：先是一个线性内积，再经过 Softmax，然后你挑选激活程度最高的前 K 个专家。这些专家的输出会被门控处理。根据实现方式的不同，你可能会根据路由权重对输出进行加权，也可能不会，然后你输出加权平均值，或者直接求和，这取决于 MOE 的具体实现。许多 MOE 论文和方法都使用了 Top-K 路由，比如 Switch Transformer、GShard、RoBERTa-Mixer、RealFormer、DeepSeek 的各种变体，它们使用的 Top-K 变体略有不同。

有一个非常令人惊讶的事实，这应该能让你深思 MOE 内部究竟在发生什么：很多结果表明，你其实完全不需要一个智能的路由器！你可以在最底层用一个哈希函数将输入 X 映射到各个专家。即使你用的是哈希，完全没有语义信息，基于哈希的 MOE 仍然能带来性能提升。这相当出人意料。MOE 领域早期的一些工作曾设想使用强化学习来学习路由行为，这自上而下看是一个非常聪明、在很多方面也非常正确的想法。因为路由选择是一个离散决策，而强化学习非常适合学习离散决策，为什么不用强化学习来学习路由呢？就我所知，早期一些 MoE 工作曾尝试过，但现在已经基本没人这么做了。因为它的计算成本太高，而且你已经有稳定性的问题了，你可能不想再雪上加霜。还有一些论文探索了解决线性分配问题或最优传输风格的方案，非常优雅，但实际上，这么做的成本远超收益。这类方法在实践中并没有被广泛采用，但确实有很多人在做这类有趣的工作，试图改进路由。

现在，我可以针对这张幻灯片，详细讲解路由是如何运作的。这就是几乎所有人都收敛到的 Top-K 路由类型，DeepSeek-V1、V2、Qwen 和 Grok 几乎都完全这么做的。这里有一个细微差别：有的会直接在底部加一个 Softmax，而像 DeepSeek-V3、Mixtral、DBRX 在底部没有 Softmax，但会对 g_i 的键值做 Softmax，这只是个微小的差异。让我们梳理一下这里发生的过程，并思考它的行为。

在最底层，我们有输入，也就是对 MLP 的输入。我想取出这个残差流……

流式输入进来，然后通过我的MOE来处理。所以第一步，我得先确定哪些专家会被激活。怎么做呢？其实方法跟注意力机制非常类似。我会取我的u，也就是残差流输入，然后把它跟每个专家的e_i向量做内积。这些e_i向量是一种可学习的向量，每个专家都有一个，它告诉专家……你知道的，指向这个方向。我计算这个内积，得到专家与输入的亲和度，然后通过一个softmax函数，为每个token确定哪些专家最合适。我将其归一化，得到s_i_t。接着，我对s_i_t执行一个top-k操作，只保留k个最大的权重，其余全部置零，以此作为门控信号。然后，我用这些权重对各个专家的输出进行加权平均，再把它加回原来的残差流，最后返回结果。希望大家对这部分都很熟悉了，本质上跟你们熟知的Transformer工作机制一样，唯一的区别就是这个top-k路由部分。大家都清楚它的工作原理了吧？很好，太棒了。

所以从某种意义上说，路由前向过程的机制其实非常简单。真正让人感到神奇的是，模型居然能把这种路由机制学得这么好。毕竟，要让模型学好并做到出色，这本身是一组相当复杂的事情。是的，我认为对softmax的一种理解方式是，它的全部目的只是为了让后面专家输出加权平均时，权重之和为1。别把softmax真的当作一个“softmax操作”，尽管名字是这么叫的。实际上，softmax操作就是一个归一化为1的操作，而归一化为1就是为了能做一个加权平均。另外很重要的一点是，你可能会想，为什么不能直接把top-k去掉？为什么不直接用这里的softmax来门控所有专家？那样的话，你会立刻丧失系统的效率优势。在训练时必须有top-k，否则你就得为所有N个专家付出训练成本。这就是MOE的关键之处：我们必须做所有这些复杂的操作，以确保在训练和推理时，激活的专家数量是稀疏的。这就是我们采用top-k的原因。

好的，后面的问题。如果先做softmax然后再取top-k得到权重，你就失去了方向。问题是这样的：如果你先做了softmax，再取top-k得到权重，那么权重之和就不再是1了。没错，确实不再为1。不过从某种角度看，并没有必须要求和为1的要求，因为下一层可以通过缩放把数值拉回来，各处都有层归一化，并非一定要求和为1。但我认为这就是为什么一些其他架构基本上把softmax的位置换了。关于是否真的希望权重之和为1，这更像是一种审美选择，也许这样对学习更有利。

需要解释一下，这里的e向量跟前馈网络有什么关系？问题是e向量是否跟前馈网络有联系，其实它们并没有直接绑定。e向量只是路由器自己的可学习参数，你可以把e看作路由器的参数，它们跟专家的前馈网络是分开的对象。

好的，另一个问题：这种方法跟从softmax中采样相比如何？你可以从softmax中采样，事实上有些方法确实会做某种软采样。特别是谷歌的一篇论文中有一种做法，他们取softmax中最大的那个，然后从剩余的softmax概率中按比例随机采样第二个。这样做能带来更多的探索性，这是好事。但缺点是，如果测试时不再进行这种采样，就会造成训练和测试的不匹配。

好，下一个问题：为什么不把所有的……也读出来？问题是有些模型确实会读取所有内容。没错，有些模型的架构做了这种选择。但这并没有太大影响，因为缩放可以在后跳连接后进行调整。所以，并没有要求在门控操作之后必须和为1。

哦，抱歉，还有。这并不是一个期望，因为每个FFN是不同的，所以这其实不是一个期望，而且门控是稀疏的。这更像是在k个不同专家（其实是总共N个不同FFN）上的一个加权选择操作。然后最后的u_t，如果你还记得Transformer的结构，那是残差流。我把输入加回去，是因为我希望通过它保留一个恒等连接。

好的，还有一个问题：为什么路由器这么基础？如果把路由器变得更复杂会怎么样？好问题。路由器为什么这么简单？似乎如果要使用专家，把每个token路由到正确的专家非常重要，那为什么不做得更复杂呢？我认为，一些早期的谷歌论文中确实有过一些探索，比如使用MLP路由器等更复杂的结构。我觉得这里比较复杂的答案是，系统层面的考量占了很大比重。如果你在路由决策上花费了大量的FLOPs，你必须为这些FLOPs付出代价，那么你就得单独从路由本身获得性能提升。还有一点需要理解的是，路由机制能学得多好其实存在很大的限制，因为这个路由过程的学习本身就相当艰难。因为你怎么才能获得哪些路由好、哪些路由坏的梯度信号呢？你唯一能利用的就是，如果你使用top-2，那么你可以对比你选中的这两个，并将梯度推入s_t中，因为你的g是一个权重，然后s_t可能影响内积。但这是学习亲和度的一个非常间接的方式。所以，即使把路由器变复杂，也不能保证你一定能学到最优路由。

好的，我认为DeepSeek MOE的一个重大创新——而且很快被其他中国的MOE发布版本采用的——就是共享专家和细粒度专家的概念。最初提出的基本MOE结构，是拿你的密集架构，然后把专家复制多份。比如，如果你有top-2路由，那么你激活的参数量会是原来密集模型的两倍。你可以把这种看作是“原始”或“基本”的MOE起点。人们很快意识到，拥有很多专家是好的。而在这个基础上逻辑上更进一步就是：我想要很多专家，但我不想要为很多专家付出那么大的参数成本。于是，DeepSeek基本上论证了正确的做法是把专家切成更小的块。回忆一下上节课我告诉你们的，在某种程度上的一条黄金法则：你的隐藏层，乘以4就得到了你的投影层。那么现在你要做的是，不是乘以4了，而是可能乘以2。这样你就得到了更小的矩阵，有了更细粒度的专家，你可以有双倍数量的专家。你还可以把这种逻辑推到更极端，比如乘以8或者四分之一，不断减小投影维度的尺寸。这就是细粒度专家。当然，这也有缺点，我稍后会讲。它并不是没有代价的，所以你必须非常小心地构建这些结构。

另外一件被研究和注意到的点是，或许至少有一些MLP能够捕捉共享的结构是有帮助的。也许有一些处理总是需要发生的，无论你处理哪个token。在这种情况下，做所有这些路由工作、把参数分散得到处都是似乎是一种浪费，而我们本可以只用一个（或少数几个）共享专家，其任务就是处理所有这些必需的共享计算。于是就出现了共享专家。所以，这种使用细粒度专家和共享专家的设置，最初出自DeepSeek MOE，尽管我认为最初的灵感来自DeepSpeed MOE，以及Qwen等。自DeepSeek之后，几乎所有开源的MOE版本都采纳了这些创新中的一部分，因为很明显的，尤其是细粒度专家，实在是太有用了，到了今天这几乎是一个无需动脑就应该采用的做法。

我很喜欢读DeepSeek论文的一点是，他们会做消融实验。这不是那种随便写写的销售技术报告，他们真的关心自己的方法是否有效。在DeepSeek MOE的论文中，他们有一个漂亮的消融实验，展示给你看：这里的蓝色条是GShard，这是一种非常基本的原始MOE实现；你可以有一个共享专家，就是橙色条，这在某些任务上带来了很大的增益，而在另一些任务上没有增益；你可以有细粒度专家，就是绿色和橙色的条，这进一步带来了增益。如果你把蓝色和橙色对比，组合所有这些差异能带来相当大的提升。我们可以看到，更多的专家和共享专家总体上似乎是有帮助的。

好的，有问题：当说“七个中选出…”时，是不是指“总共七个里面激活七个”？是的。抱歉我应该标注一下，它在画面下面。所以“x of y”的意思是，在总共y个被路由的专家中，激活了x个。没错。你还可以看到这里的模式：随着专家数量的增加，通常你也会增加激活专家的数量，尤其是在使用细粒度专家时。这基本上等于是免费的，因为每个专家变得更小了。

好，Allona有一些非常好的佐证，这些证据很漂亮地表明这些方法是有效的。我从最下面的一个开始说，因为它更具决定性。它展示了细粒度专家从8个增加到30个再到64个的过程，这在某种意义上复现了DeepSeek的消融实验。你可以在损失和其他一些指标上看到非常明确的趋势，从8到30到64，都有改善。细粒度专家很棒。共享专家，就是最顶部的紫色和青色对比，实际上在Allona的设置中几乎没有看到任何增益。所以，他们最终选择了不使用共享专家，尽管DeepSeek的论文似乎显示了更多的增益。因此，鉴于这种第三方对这类想法的复现结果，共享专家这一项实际上可能更复杂，结论更不确定。

说到这里，你们可能会想知道，常见的配置是怎样的。我想我会像上节课那样，看看最近发布的各种模型，观察人们的做法，并试着谈谈其中的模式。

嗯，没有特别的理由。所以一些早期的谷歌论文，像G-shard、Switch Transformer、ST-MoE，其中有些使用了数量非常庞大的路由专家，论文里有很多很有趣的内容，鼓励大家去读一下。有些工作是在LSTM或其他架构上完成的。不管怎样，很快大家就会看到，有一段时间流行使用8到16个专家，比如混合路由的DVRX、Grok那就用2个激活专家，这些方法效果还算可以。但随后DeepSeek MoE或者说DeepSeek MoE V1出现了，它采用了我之前讲过的典型配置：细粒度专家，总共64个，其中6个是主动路由的，2个是共享专家。每个专家的大小大约是标准专家大小的四分之一。对于最后一列的数值，请谨慎参考，因为我是从配置文件中反推出来的，所以对这些确切比例不是百分百确定。之后我们看到了DeepSeek V3、MiniMax，这些都是中国的MoE模型，它们在思路上基本沿袭了DeepSeek V1的道路。虽然具体数字不同，但从使用细粒度专家和经常带有共享专家这一点来看，它们与最初的DeepSeek MoE配置非常相似。Olmo、MiMaX和LLaMA都是非常近期的MoE模型，它们肯定都采用了这些细粒度专家的方法。LLaMA 4也使用了共享专家。你会看到配置上的一些变化，但基本上共通的一点就是这个细粒度专家的思路。尤其对于像LLaMA 4和DeepSeek这样的大模型，路由专家的数量非常大。抱歉，不是路由专家，是总专家数非常大。是的，专家数量比例是扩展的，它大致表示的是，相对于标准的稠密配置，每个专家被切得有多细。所以，从超参数的角度看，如果你遵循经验法则，你的隐藏维度和内部MLP的投影尺寸应该大约是1:4，或者如果是门控网络就是1:2.6。通过观察这些架构的隐藏层，你就能大致看出他们把原始的FFN尺寸切了多少份。你可以这样想，这大致相当于有16个标准大小的专家。它们当然比等价的稠密模型拥有更多参数。它们有6个路由专家，所以任何时候总共有8个激活专家，每个是四分之一大小。你应该把它们看作大约是同等稠密模型浮点运算量的两倍。这里有一些算术，但希望计算是清楚且一致的。对于某些奇特的比率，我不太确定为什么是那样，但当你计算FFN尺寸和隐含超参数之间的比率时，它们恰好是非常精确的整数。所以我觉着那些正是它们被切分成的份数，但我不确定为什么会有十四分之一这样的数字，就像......就像一个...就像活口扳手一样，就是......我们觉得应该是那样。嗯，是这样啊。这就是为什么你在问他们是否进行了下采样。我会去查一下，有些模型确实没有使用，我不记得具体是哪些模型了，但我记得有些模型里它们确实是进行了下采样的。这信息有什么用呢？能坐不止一把椅子，这挺有价值的，是吧。我的意思是，看起来确实有一段时间，一些其他的中国大模型公司尝试了多个共享专家，然后人们又回到了零或一个共享专家。如果你看所有消融实验，甚至是否一个共享专家都有明显用处，这一点并不十分清楚。我想最初的动机是，那样就能让大家拥有大小相等的专家，比如这些都是四分之一大小的专家，然后总共就有8个激活专家，这样可以保持大小一致。否则，我真的看不出有什么特别的理由要用两个较小的而不是一个较大的。

好，那么希望你明白了这些MoE的路由是如何工作的，以及整个前向传播是如何设置的。现在应该完全理解了。接下来我们需要考虑训练。训练是相当棘手的问题，我之前也暗示过主要挑战。训练时我们不能打开所有专家，因为如果那样做，我们就要付出所有专家的全部浮点运算成本。让一个模型的训练成本高出256倍，是绝对不行的。所以我们需要训练时的稀疏性，但稀疏路由决策显然是不可微的。我们现在就有了一个有点烦人的、类似于强化学习的问题。所以我们可以采取这些方法中的任何一种：比如用强化学习来优化路由策略；可以做一些受赌博机启发的事情，引入随机性来进行探索；或者，我们可以直接使用一些启发式方法，尝试让负载平衡，比如加一些损失项进去，希望事情能顺利进行。如果你上过很多种深度学习课程，心里大概能猜到实践中人们用的是哪一种。我将依次讨论这三种方法。

好的，强化学习，我认为这是人们最早尝试的方法之一，可能也是这个领域里最有原则性的方法。你有一个不可微的路由决策，那就把它看作一个策略，通过强化学习来解决。不幸的是，它并不比其他很多方法更好。Clark等人2020年有一篇论文，在探索MoE的各种缩放相关问题，他们确实做了一个强化学习基线，我把它找出来了。但不幸的是，它并不比用哈希来做决策好多少。他们对左边那个叫S-Base的方法很感兴趣，这是一种线性分配方法，那个方法居然手工设计得比用强化学习还要好。我认为，实际中的梯度方差和复杂性意味着它用起来相当不稳定，而且据我所知，在大规模训练中，没有人真正使用过基于强化学习的方法来优化这些门控决策。

我认为，在大规模实践中应用得更多的是各种客户端的随机近似方法。他们可能会加入一些扰动。这里是Shazeer等人在2017年提出的一个例子，那是早期一篇MoE论文。他们当时还是要做一种top-k路由，所以他们会保留这个H(x)运算中的top-k个元素，我的意思是，他们会对它进行softmax来得到门控值。但为了得到这个H(x)运算，他们要做的如下：我们有原始的线性亲和度，这和之前做的一样，基本上就是计算输入X和一个为每个门学习的权重，这部分是一样的。但我现在要给它加上一点抖动，添加一个正态分布，然后选取一个可学习的W噪声尺度，这个尺度将控制向这个过程中注入多少噪声。你可以把这看作一种随机探索策略。通过以特定方式操控W噪声，比如把它衰减下去或者做其他操作，我可以控制这个MoE模型面临的探索与利用之间的权衡。这为探索利用困境提供了一个解决方案。特别是，如果你给事情引入噪声，每个专家可能会随机地拿到一些它不曾预期会拿到的token，这样就会迫使专家们不那么特化，但也许会更鲁棒一些。这看起来通常很不错。当然，这种随机性也意味着你无法获得那么高的特化程度，这会导致效率损失。还有另一种方法，人们将路由器logits乘以某种扰动，目的是让专家不那么脆弱。但这种抖动的过程在后来的一些论文中被去掉了，因为他们发现它不如一些基于启发式损失函数的方法效果好。所以，这种随机路由技巧在早期的一些谷歌论文中被尝试过，但我认为，现在很多训练这些MoE的人已经普遍放弃了这种方法。

好的，那么对于随机方法，它解决的是什么问题？就像纸杯蛋糕里的随机性。我们还得听听你对这点的看法。问题是，我们仍然无法微分，因为我们要取top-k。但如果你稍微改变一下对问题的理解，把它想成一个赌博机问题，它的结构和我们面临的一样：你拉下一个赌博机臂，就看不到其他臂的结果了，所以你无法有效地分配资源。如果你随机地去拉一些其他的臂，现在你就有足够的数据来进行一些优化了。所以这种抖动在精神上非常类似于epsilon贪婪式的探索策略，你以某个概率随机地拉下其他一些臂，而这个概率本身取决于你对当前路由决策的信心程度。这就是其直觉所在。然后，当然，这会给你提供一些获取反馈信号的方法。

好的，在实践中，人们最终采用的方法其实并没有上面那些。我们不做强化学习，不做随机探索，而是依赖另一种机制来让事情保持合理。如果我们做top-2路由，严格来说，我们确实在梯度下降过程中得到一些信号，因为我们可以比较我们实际计算的那两个专家的结果，所以进行一些优化是可能的。但是，如果我们忽略所有其他约束，出现的主要问题就是，你最终会倾向于总是选择一个专家，那个专家变得什么都会，而其他所有专家都表现糟糕。你陷入了一个局部最小值，其中你将所有的token一直路由到一个专家。所以，真正的关键博弈就变成了如何跳出那个局部最小值。负载均衡损失，或者说平衡损失，就是用来跳出这个困境的关键技巧。理解这点很重要，因为这是几乎所有人实际用来训练MoE的损失函数。所以，如果你之前走神了，你现在可能应该确保注意一下这组特定的方程。

这最初来自Fedus等人在2022年提出的Switch Transformer。他们添加了这个特定的损失函数，它的做法是，循环遍历每一个专家，然后取，你可以把它看作向量f和向量p的内积。那么这些向量是什么呢？f是针对每一个专家，分配到这个专家的token的比例。你可以把它看作一个概率向量，告诉我，在我的批次或无论什么单元里，我路由给专家i的token比例是多少。而p(i)是分配给专家i的路由器概率的比例。路由器概率就是最初打算发送的softmax路由决定。所以，这在衡量的是：p(i)是“意图上”想发送给每个专家的比例，而f(i)是实际上最终分配给每个专家的token比例。

从路由器获得的概率，然后f(i)是实际通过top k方法做出的路由决策。这里有一个有趣的地方：如果我们对该损失关于p(i)求导，你会发现这是一个关于p(i)的线性函数，而且最大的“下压”作用会发生在那些获得最多分配的、最大的专家身上。也就是说，它实际上与你获得的token数量成比例。因此，如果你获得了更多token，你就会受到更强的向下推力。这就是这种损失的基本行为特征。几乎所有人都会用这种f点乘p的小技巧，来尝试在不同单元之间平衡token分配。

最初你可能希望平衡的基本单位是批次，你希望每个批次能均匀地分配给各个专家。但实际上你可能还有其他需要平衡的方面，而DeepSeek正是这么做的。我会谈到他们加入的所有变体。首先就是每个批次内的专家间平衡——在每个批次中，他们希望确保各个专家获得均等的token数量。这来自DeepSeek的论文，希望你们看起来很熟悉，这和你们之前看到的f点乘p的内积结构完全一样。p(i)的定义略有不同，它是s(i,t)，但你们之前也应该见过，就是top k之前的softmax值。希望这一切你们都觉得清楚。

另外你可能还想要的，就是不仅要考虑专家间的平衡，还需要考虑系统层面的问题。因为你会把专家分片到不同的设备上，你可能也希望在每个设备之间进行平衡。于是你可能有另一个结构完全相同的损失函数，但不是统计哪些token去了哪些专家，而是统计哪些token去了哪些设备。这将是另一个基于设备组而不是基于每个专家计算的f。这样你就可以设置一个新的损失来平衡设备间的负载。通过优化这个损失，你自然会学到能让每个GPU或每个设备获得均等token数量的路由函数，从而实现均匀的利用率，从系统角度看这非常棒。所以基本上所有人都在做这类事情。

而DeepSeek V3其实做了一些小小的创新，这挺酷的，我之前好像没看到过。这是MoE领域里很少见的并非源自Google的创意之一。他们完全去掉了这个专家平衡项，彻底摒弃了它。取而代之的是，他们在softmax分数上加上了一个小的修正因子b(i)。b(i)是每个专家的一个小偏移分数。比如专家i可能会被向上加权或向下加权。如果一个专家没有得到足够的token，它就会被赋予一个更高的b(i)，从而能够捕获更多token。它的工作原理是：他们会用一种非常简单的在线梯度方案——在线学习——来学习b(i)。在每个批次，他们会观察每个专家获得多少token，是否均等。如果某个专家token不足，他们就会给b(i)加上一个学习率γ，使其变得更高；如果某个专家token过多，就减去γ，使其吸引力稍降。所以他们只是为每个s(i)学习一些小的偏移量。请注意，这里b(i)仅用于路由决策，并不会作为门控权重的一部分发送出去，这一点相当重要。他们称之为“无辅助损失平衡”。如果你们去读DeepSeek V3的论文（你们都应该去读，因为确实很好），他们会大力宣扬这种方方法让训练变得多么稳定、多么出色、多么美妙。然而，当你继续往下读这一节，他们会说，实际上，但对于每个序列，他们可能仍然希望保持平衡，而这种方法效果不够好，于是他们又把启发式损失加回来了。所以他们有一个被称为“互补序列级辅助损失”的东西，基本上就完全是那个辅助损失。他们觉得需要它，是因为他们想要在序列级别而不是批次级别上进行负载均衡。我不太确定他们为什么选择这种方法，而不是其他某种b(i)风格的技巧，但这就是DeepSeek V3里的做法。所以它并不像他们想让你相信的那样完全不需要辅助损失。

好的，有提问。这很有趣：如果我们不必担心系统优化，你认为这个模型的性能会好很多还是会大致相同？如果我们不考虑系统优化，模型的性能会更好还是保持原样？你问的“这个模型”是指DeepSeek V3还是更广义的这类模型？你的意思是不是说，如果我们忽略系统层面的问题，MoE仍然会是好的，还是说我们能用某种非流式的方法做得更好？是的，我觉得我必须对此做出平衡，就像必须大致均衡分配给各个专家一样。没错，确实是这样。我认为，正确的专家平衡这一项，这本身并不是系统层面的问题，你仍然需要做这件事。因为如果你不做，你会发现——我还是继续引用那篇经典的论文吧，他们有很多消融实验——他们有一个很好的消融实验，正是去掉了这个损失。他们发现，在训练初期，模型会只挑选一两个专家，而其他所有专家都“死”掉了，路由器从不发送任何token给它们。那你就只是在浪费内存，因为此时你实际上得到了一个更小的模型，白白损失了性能。所以即使你忽略所有其他的设备平衡之类的顾虑，你也只是得到了一个更差的模型，因为你没有合理地分配你的专家。这就像你希望利用所有参数一样，你会希望有效利用你的参数，所以你需要做专家平衡。

追问：所说的“设备”指什么？通常这指的是GPU或TPU。这里有个微妙之处，我可能会在最后一两节课讲。有更复杂、更酷的版本，你试图平衡负载以最小化通信成本，所以设备的概念会更广，比如一个机架之类的。但这里通常指的是GPU。

为什么哈希还能起任何作用？我对这点没有非常精确的直觉，但你可以从两方面论证。一是，即使你进行哈希，相同的token或同类的序列每次仍然会去往同一个专家，所以每个专家仍会得到输入的某个确定性子集，因此仍然可以发生某种专业化，只是非语义的或非学习的。如果你的分布是齐夫分布，可能某个专家会被非常高频的词主导。但如果是与输入无关的固定或随机哈希，我打赌那会非常糟糕。是的，我没有测试过，但我觉得那样会非常糟糕。

那么对于很多层和很多专家的情况呢？实际上，之前课上有提到，即使有32层，每个专家被激活的次数也是可以的。如果你的多个专家挤在一起，你能做的是……追问题：当你需要很多GPU，有很多层和专家时，如果你把一个GPU专门分配给一个专家，那确实会有些疯狂。但你会分摊，让每个GPU承载足够多的这些单元，以有效利用内存。并行计算的核心就是始终要充分利用内存，因为那是资源之一，你不想做超出必要的并行。

我在幻灯片里放了这个消融实验。这正是回答那个问题的：如果不做专家平衡损失会发生什么？最值得看的图是左下角那张。如果你不做负载均衡，token分配情况是怎样的？你会看到粉色和黄色的专家几乎占据了主导，拿走了大约50%的token，而其他专家全部“死掉”，什么也不做。所以你浪费了大部分专家，比如八个专家中有六个被浪费了，无意中创建了一个只有两个专家的MoE。这导致右上角的损失曲线（实线）变得更差。当然，也许那仍然比密集模型好，因为你至少还有两个专家在工作。但本可以做得更好。

好的，那么我不太会深入到系统侧，因为还没开始讲理解并行计算问题所需的核心系统概念，比如数据中心通信速度的层次结构等等。但正如我之前所说，有一件事要记住：MoE能很好地适配设备。人们常说的“专家并行”，是将一个或几个专家放到每个设备上。当你处理一个token时，你会先经过路由器，路由器选定了少数几个专家，然后你就会进行一种称为“全对全通信”的集体通信操作，将token分发到相关设备上，进行前馈计算，得到输出，然后再将token返回到它们应该去的地方，或者结合多个专家的输出。这就需要另一次集体通信调用。如果你的前馈计算足够庞大、足够耗时，你就可以摊销掉这种专家并行带来的通信成本。它的好处在于，这为你的工具箱增加了另一种并行方式。右侧你已经有数据并行、两三种模型并行，再加上专家并行，你就可以把它们结合起来，权衡你所拥有的各种资源，比如通信速度、数据量、批次大小、专家数量以及内存等等。

好的，那么……关于这具体会带来什么帮助，我就不展开太多细节了，但请记住，这为你的专家工具箱又增添了一种工具。另一件……同样有用的事情是，假设你在单个设备上有多个专家。你可能会希望，因为竞争是稀疏的，比如第一个 token 与专家零相乘，第二个 token 是专家一，第三个是专家二。所以这实际上是三个小规模且稀疏的矩阵乘法，你可能会希望现代 GPU 能够利用这些复杂的稀疏矩阵乘法特性……没错，完全正确。如果你以正确的方式排布专家，并且权重以适当的方式融合，那么现代的稀疏矩阵乘法引擎可以有效地确保你在进行这种大矩阵乘法时不会浪费任何 FLOPs。因此，像 MegaBlocks 这样的现代库，基本上可以利用这种设备级的稀疏性支持，一次性完成所有专家竞争的计算。这就是 MOE 带来的又一个优势。

还有一件有趣的题外话，你可能已经不觉得神秘了，因为你是 GPT-4 时代成长起来的……但当 GPT-4 API 刚推出时，对我来说有点神秘，因为当我把温度设为 0 时，按理说应该是确定性的，但你却会得到不同的回复。很多人都在猜测为什么会这样。我并不是说这就是原因，但在 MOE 中确实存在一个有趣的随机性来源。想想看，在 MOE 中，你要把 token 路由到专家那里去，而专家们分布在不同设备上。可能会有很多请求，你当然会在处理时进行批量查询。如果你对这些查询进行了批处理，这些 token 就会被路由到不同的专家那里。想象一下，你进行了批处理，有一堆专家，但出于某种原因，这个批次特别钟爱专家三，几乎所有的 token 都去了专家三那里。接下来会发生什么？专家三所在的设备没有足够的内存来加载所有这些 token，然后就会出现所谓的“token 丢弃”现象。这在训练时也会发生，通常你会设置一个负载因子，用来控制允许的最大 token 数量，如果路由器把太多 token 分配给某个专家，你就会丢掉那些多余的 token，这可能是出于系统原因，或者因为担心那个专家会主导训练。于是，这个 token 就被丢弃了，它什么也得不到，MLP 只会做零计算，然后残差连接直接把它传过去，最终返回一个输出。因此，如果你的 token 被丢弃了，得到的结果就和没被丢弃的不同。所以，基于你的批次中还有哪些其他 token，MOE 可能会在训练和推理时引发容量问题，这是一种你通常不会想到的有趣情况，因为在进行推理时，你几乎从来不会考虑跨批次效应。

好，以上就是构建 MOE 的一些主要基本组成部分，以及一个有趣的侧记。如果你明天真的去尝试训练一个 MOE，我想系统方面会让你有点头疼，但另一件让你头疼的事很可能是稳定性方面。MOE 有时会有这种特性，如果你尝试对它们进行微调，它们可能会突然崩溃，微调它们非常困难，有时会突然崩溃。于是，研究者们，比如 exhaust 等人，就专门研究了一篇论文，其全部目的就是让 MOE 训练更稳定。我会提到几个我认为相关且大家都在使用的技巧。第一个是，如果你在做路由器的 softmax，这就要回到上节课关于稳定性的内容了，对吧？我关于稳定性说了什么？最需要担心的就是 softmax。所以，对于 MOE，他们在路由器的计算中全部使用 float32，只是为了安全起见。有时他们还会添加辅助损失或 Z 损失。希望你还记得，就在上节课，你计算 softmax 中指数值的和的对数，然后将它平方，作为额外的损失项加进去。这样可以使归一化值保持在 1 附近，有利于稳定性。实际上，Z 损失就是在这类场景中早期得到应用的，后来才在模型训练中变得更流行。你可以看看损失图的效果，我觉得中间的第二张图可能很不错：如果你从路由器功能中去掉 Z 损失，就会在验证损失中看到这些巨大的损失尖峰，模型在某些迭代中会有点“发疯”，然后又被拉回来，当然它仍然能训练，但有 Z 损失比没有要好，到最终验证损失存在明显的差距。

其他可能发生的情况是，你当然想微调你的 MOE，如果你想发布它，可能还想对它进行 RLAIF。但这被证明有点问题。在早期工作中，那时人们开始做 MOE，大约是在 BERT 和 T5 的时代，所以有很多微调工作。当时人们发现，如果你在做稀疏模型，实际上会出现很多过拟合现象，你会看到训练和验证之间的巨大差距，就像这条蓝线和橙线。而密集模型，这条绿线和红线，其训练-测试差距较小。因此，当时有很多关于过拟合的担忧，因为你在小数据集上微调的是这种参数规模巨大的模型。当时提出的一个解决方案（据我了解，现在并不流行）是，将你的 MOE 架构设计成并非每一层都是 MOE 层，而是比如交替使用密集层和 MOE 层，然后你只需要微调密集层就行了，这样就没问题，行为就像密集模型一样。这就没问题了。另一个解决方案，我们在 DeepSeek MOE 论文中看到的，就是使用大量数据。如果过拟合是一个问题，那么我们就有海量的 SFT 数据可用，把这些数据全部灌进去。在 DeepSeek MOE 的案例中，他们使用了 1.5 万亿个训练样本，这样或许你就不那么担心过拟合问题了。

我最后要讲的一个技巧，是工具箱中的一个，有人做过并验证过的，那就是“向上循环”。这个想法是，先拿一个密集模型，比如我这里画的那个，然后你取它的 MLP，复制多份，可能再加一些扰动，然后你的路由器从头开始初始化，接着你就把它当作一个 MOE，从那个起点开始训练。也就是说，你从一个密集模型初始化 MOE。这个技巧被称为“向上循环”。并且，人们已经证明，如果能让它成功运行，这是一种极其经济高效的获得 MOE 的方法。而 MOE 对于推理非常有利，因为并非每个 MLP 在推理时都会被激活，所以你实际上可能得到一个参数规模大得多的模型，却无需进行大模型从头训练。已经有一些人在这方面取得了成功。MiniCPM，我在缩放定律那一课还会提到，这是一个中国的开源语言模型项目，他们试图构建非常好的小语言模型，并且成功地从一个密集模型向上循环训练出了一个 MOE。你可以看到，在表格的最后两行，他们的性能显著提升，从密集模型到 MOE，性能提升相当可观。Qwen，我在本课开始时提到过，他们最早尝试 MOE 的做法之一，就是取一个密集模型，然后构建向上循环的 MOE。相比于当时规模较小的模型，他们获得了相当显著的性能提升，比如他们用一个 27 亿激活参数的模型，达到了与 70 亿模型相当的性能。

所以，总结一下，我想在最后部分快速过一遍 DeepSeek MOE 的架构。希望这能让你有所体会。首先，我希望你理解 DeepSeek V3 架构的配置以及他们所做的所有改动，因为那是一个现代高性能开源系统的例子。同时，我也希望你能够体会到，架构实际上变化并不大。DeepSeek V1，或者叫 DeepSeek MOE V1，并不新，可能是一年半或两年前的东西了，而在那个时候他们基本上就敲定了架构。所以我想让你们看看，从那个非常早期的尝试，到他们的大规模训练运行，到底改变了什么。

这是最初的起点，我称之为 DeepSeek MOE V1，但正确的称呼可能应该是 DeepSeek MOE。它是一个 160 亿参数的模型，其中 28 亿参数是激活的。你们已经见过这个图了，这里有 2 个共享专家加上 64 个细粒度专家，其中每次有 4 个（抱歉，是每次 6 个）专家是激活的。路由机制你们也见过了，我在讲座中间展示过，就是非常标准的 top-k 路由，在 top-k 选择之前底部有一个 softmax。在训练时，为了做均衡，他们所做的就是添加辅助损失均衡项，包括专家级和设备级均衡项。希望你们还记得之前的内容。这就是 V1。

后来，他们看到了 MOE 模型是多么有效。顺便提一下更多背景：DeepSeek 最初有一个密集模型，然后做了一个 MOE 模型，这个 MOE 模型表现极其出色。所以当他们做 V2 时，直接选择了 MOE。现在这是一个 2360 亿参数的模型，其中 210 亿参数是激活的。所以你需要很大的内存，但推理这个模型时的 FLOPs 消耗还可以，不算太糟。架构是完全相同的。我直接复制了同一张图，因为架构几乎一模一样，除了激活专家数量的变化。现在我们看到了一些新东西，但不是很多。top-k 选择器是一样的，之前那个公式，是完全相同的，仍然是这样运作。但他们加入了一个非常巧妙的技巧，这个我本来想在开头讲的：你可能会问，拥有细粒度专家有什么缺点？为什么我不能有比如 1024 个细粒度专家？

训练专家或2046个细粒度专家时，问题是当你非常精细地分片专家，并且有大量活跃的专家时，你就必须将token路由到这些专家，因此通信成本可能会增长。而如果你非常碎片化，你可能需要将大量token发送到许多设备上，对吧。所以他们想出的巧妙方法是：我不再天真地针对每个批次，将token路由到top-k个专家，那样可能会迫使我向大量设备发送token。我要做的是，首先选出top-N个设备，对，我会进行常规的评分计算，但我首先要把允许的设备集合缩减为top-N个，一旦选定了设备，然后再在每个设备内部为每个token选出top-k个专家。这样我就限制了设备数量，真正控制了通信成本，并且当扩展到如此庞大的规模时，这会带来更高效的训练。当你训练2360亿参数的主模型时，你真的需要开始认真处理系统方面的问题。另一件反映这种规模下必需的系统关注点的事情是，他们添加了一个通信均衡损失。一种思考方式是，对于专家而言，有输入和输出：输入就是token进来，你将它路由到专家；输出就是你得把token带回到原本的地方。也就是说，如果一个批次属于这个设备，它就得回到原来的设备上去。因此我们必须同时考虑输入通信成本和输出通信成本。所以他们添加了一个均衡损失，试图也平衡输出通信成本，而不仅仅是输入方面。这只是一个次要的说明，但你可以看到他们对细节的关注，力求确保所有不同的系统方面都得到妥善处理。

最后我们来到了大的DeepSeek V3，对不起，上面应该写V3而不是V2。6710亿参数，其中370亿是活跃的。再一次，完全相同的数据，因为MoE架构本身没有改变，这从DeepSeek MoE以来就一直如此，就像如果有效就不要改。他们确实改变了一些东西。也许他们听到你们都在说为什么不归一化到1，所以他们把门控值归一化到1，他们将softmax归一化操作移到了上面，但他们实际上并没有进行指数运算，对于门控决策，他们实际上使用的是sigmoid函数，这是一种更柔和、行为更良好的操作，比softmax更好。所以他们有些变化，但概念上仍然与top-k路由决策相同，希望你能看到非常相似的情况。

在损失方面，他们采用了一种辅助损失自由的技巧：根据专家负载对偏置项B_i进行增加或减少，然后还有一个序列级别的辅助损失。再补充一些背景：为什么要在单个序列上平衡不同的专家？他们非常担心的是，在训练时，没有序列级别的平衡损失倒还好，但在推理时，可能会有人发送非常偏离分布的序列，这就有可能淹没某些专家。所以，在推理时你无法控制得到什么序列，因此你可能需要更强的平衡机制，让它在单个序列级别上起作用，而不是整体的批次级别。

那么，它还会做top-N设备的选择吗？是的，他们保留了top-N的改进。例如，他们没有保留那个通信损失，所以他们丢弃了一些东西，但top-N似乎是相当聪明的想法，他们保留了它。但这并不是说他们总是增加东西，他们也移除了一些。

所以，在课堂的最后大约两分钟，我将回顾DeepSeek V3中非MoE的部分，因为我觉得我们已经准备好，我已经讲解了DeepSeek V3的大部分内容，不妨趁此机会把剩下的部分也讲解完，这样大家就知道它是如何工作的了。他们针对注意力部分有一个巧妙的优化，叫做MLA，即多头潜在注意力。你们其实已经掌握了理解它所需的所有要素，因为在上一次课的最后，我讲了GQA和MHA，这些都是为了优化KV缓存大小而需要的推理优化。DeepSeek团队采用了不同的方法进行优化，不是减少头的数量，而是将头投影到一个更低维的空间。

你有输入 H_t，与其直接从这些 H_t 生成 K 和 V，我将首先生成一个低维的 C，你可以把它看作是 H 的压缩版本，C 会更小、更容易缓存，我就缓存这些 C。每当我需要这些 K 和 V 时，我可以从这个 KV（概念上）进行上投影，然后与 Q 做内积。所以你可以看到，如果我只保存 C 而不是更高维的 H_t，这将节省 KV 缓存。这正是其核心思想：取 H_t，将其投影到低维的 C，然后再将 C 上投影回 K 和 V。如果 C 很小，那么你就压缩了 KV 缓存，这很好。然后，就计算而言，如果你考虑 FLOPs，你可能会想，这不好，因为我必须额外乘以矩阵 W_{UK}，之前没有这个矩阵，这是额外的矩阵乘法，我得为此付出代价。但巧妙之处在于，请记住，在 K 的另一边，我要计算 K 与 Q 的点积，在注意力操作中，Q·K 将是内积，而 Q 本身有一个投影矩阵 Q。这里的技巧是，你可以将这个 W_{UK} 与 Q 矩阵合并为一个矩阵，所以我并没有增加额外的矩阵乘法，我只是把这个新的矩阵乘法融合到了另一个中去，这就是结合律，我可以合并这两个。

他们还压缩查询以便在训练时节省内存，但实际上这个压缩并不那么必要，因为它与KV缓存完全没有交互。我只顺带提一下最后一点，因为这是一个微妙之处，但却是一个你意识到的巧妙细节：就是我刚才描述的那个原始技巧与RoPE不兼容。原因在于，RoPE矩阵，基本上你是有Q和K，然后通过乘以旋转矩阵R_Q和R_K来旋转每个Q和K。但如果你这么做，这些R_Q和R_K就处于查询投影和这个潜在向量上投影矩阵之间，由于我无法重新排序这些矩阵乘法，RoPE就有些碍事了。他们仍然有一个解决方案，基本上是在非压缩维度上做RoPE。这算是一个次要的点，我觉得没那么重要，如果你非常感兴趣可以去看论文。

他们做的另一件事，我保证这是最后一点，是他们在损失函数上做了一个小改动，叫做MKP，即并行预测多个token。他们能这样做：通常，你有你的输入，将其左移一位，你就在预测未来一个token，然后你的Transformer会预测所有这些token，这是正常的Transformer损失。但你可以做的是，就在进行这些预测之前，你可以取出隐藏状态，将其传递给一个非常轻量级的单层Transformer，这个模型可以预测未来的一个token。所以现在，模型不仅预测下一个token，还预测未来两个token。希望这都能讲得通。这只是一个小型的轻量级模型就能做到这一点。你可以在这里看到架构。但有一点让我有点失望，我在备课时了解到，实际上他们只使用MKP来预测前方一个token，即使他们有一张非常复杂的图表展示了可以预测多个token，结果却只做了一个token的预测。

好了，现在我都讲完了。MoE如今已成为构建和部署真正高性能大规模系统的核心，它们利用了稀疏性的思想，即你并不需要所有时刻都用到全部参数。离散路由是真正的巨大挑战，我认为这是MoE一开始未能迅速普及的重要原因之一，因为试图优化这种top-k路由决策非常可怕，但不知何故它似乎就是有效，它们确实有效。所以现在有大量的经验证据表明，至少在算力受限的情况下，MoE是一个好主意，它成本效益高，你应该采用它。所以绝对值得学习。非常感谢大家的聆听。