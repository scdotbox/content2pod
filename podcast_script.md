# Content2Pod 播客

我们已经积累了经验，准备以更高效的方式推进。现在我们要介绍一个重要的思路和方法，这与硬件密切相关。我们需要从南海的一个很好的实例出发，来介绍这个新的信息和个人工作方式。他们在这方面做得很好，我们也会看到滴滴出行的相关优化，其实也很不错。基本上，他们会在滴滴出行的环境中进行各种优化，这背后有很多理由。我们原以为资深的教师和学生能够完成这些任务，但我们需要明确到底该采用什么样的行动方案。因此，我们要使用怎样的投资和行动来推动这件事？针对这个人，我们应该采取什么行动？我个人的做法是，我会根据自己的行动方案来调整，并在思考中不断地裁剪和优化，最终让方案变得清晰可见。所以，教授、老师们如果有这样的行动方案，我们一定要强调英国这个主题。因此，我会想象我们有一个关于缩放定律的SaaS平台，你们可能已经见过这个图了。这只是一个背景介绍。我们知道，拥有更多算力对于训练大型语言模型是很有帮助的。这个图是预训练的缩放曲线，但你也可以把它换成推理的缩放曲线。大家普遍认为，拥有的算力越多，能处理的数据就越多，可以摄取更多数据，训练更大的模型，这些都能提升性能。所以，你可能认为深度学习本身非常重要，但真正推动性能提升的，其实是算力的快速增长。我们拥有更多的小型思考单元，相当于129层以上的那种计算能力，这就像是我们光方向上的新玩法，在游戏中有很多独特的优势，因此我们必须提出比较新的思路来应对。这就是当下每天、整个外部环境对我们的要求。所以，我们用自己的方式设计出比较新的影片，并应用于这种新的设计中，结果证明这样做是非常有帮助的。它并不能在单线程上提供更高的吞吐量，这意味着我们不能单纯依赖绝对速度来加快计算，而是必须通过并行扩展来弥补。因此，深度学习和神经网络的扩展历程，就是从单线程扩展（绝对意义上更快地执行计算）转向并行扩展，即同时计算大量工作负载。这是我最喜欢的内容之一，比如Bill Dowley在其主题演讲中展示的计算扩展图，其中显示了从最早的K20到H100，每秒整数运算次数呈超指数级增长，形成了一条极为显著的指数曲线。因此，我们必须真正理解如何利用这条曲线，才能从语言模型中榨取最大价值。这就是我们的目标。我已经暗示了一个重要的区别：CPU是大家一旦开始编程就熟悉的东西——它是一种执行模型，你有一个程序，然后它逐步按顺序执行单线程的操作。为了支持这种执行模型，你需要什么？你需要庞大的控制单元。因为你有很多分支和大量的条件控制逻辑，所以你需要让这些单元运行得非常快。CPU在这种抽象的架构图中，会把芯片的大部分面积用于大型控制分支预测，同时快速运行这些操作，因为它并没有那么多线程。虽然现在的CPU有大量核心，但与GPU相比还是微不足道。而相比之下，GPU拥有海量的计算单元（ALU），图中那些小绿框就是。芯片上用于控制的部分要小得多，只有少量的控制逻辑，却负责调度成千上万个并行工作的计算单元。所以，从设计目标来看，CPU和GPU追求的是完全不同的东西。CPU优化的是延迟，我希望尽可能快地完成每一个任务。比如在右侧有P1到T4几个任务，CPU会尽力让每个任务尽快完成，这样一来，任何一个任务要快速完成，比如T1很快就能结束。而GPU优化的是高吞吐量，它不在乎延迟，只希望所有任务的总完成时间越短越好。为了支持这一点，它可能拥有大量线程，这些线程可以非常快速地休眠和唤醒。最后，尽管单个任务的延迟可能更高，但所有工作负载T1到T4完成的时间可能比CPU版本更早。所以它们的设计原则和目标是截然不同的。因此，GPU的架构也大不相同。我不知道你们是否看过GPU的芯片布局图，我稍后会展示真实的芯片照片。但核心且重要的概念是，GPU中有许多流式多处理器（SM，Streaming Multiprocessor），你可以将SM视为一个原子单元。当你在Triton等环境中编程时，通常会工作在SM的层面。每个SM内包含许多流处理器（SP），一个流处理器会并行执行一大批线程。所以可以这样理解：SM拥有一点控制逻辑，可以决定执行什么，比如进行分支；而SP则采用同一条指令，将其应用于大量不同的数据。在这种模型下，你可以进行大规模并行计算。SM是细粒度的控制单元，SP可以单独执行大量计算。以A100（上一代GPU）为例，它拥有128个SM，这比CPU的核心数多得多，而且每个SM内又有大量的SP和专用的矩阵乘法单元。这就是它的计算模型。有人问了问题吗？对，问题是，这张图里的GPU和前面提到的是同一个吗？是的，这是它的卡通版本。你可以把每一行看作一个SM，它有自己的控制单元，每个绿色块可能对应这里的某个绿色块，比如内部的FP32处理单元。每个SM可以操作属于自己的部分（比如Tensor Core）来进行计算。好，继续。你需要记住两点：大家通常把GPU看作计算机，它确实在计算，但实际上计算只是我们需要关注的两个重要因素之一，内存可以说现在更加重要，而且在未来会更加制约我们在GPU上运行程序的性能。要理解内存，你不得不了解GPU芯片的物理布局。因为在如此高的速度下，内存的物理距离对性能影响很大。我会展示芯片上各个部分的物理距离以及这如何影响你对内存访问和性能的思考。内存越靠近SM，访问速度越快。所以存在一类极快的内存，比如L1缓存和共享内存，它们位于SM内部，速度非常快，比如寄存器、频繁读写的数据都应放在L1和共享内存中。L2缓存呢，你可以看到图中那些绿色区域是SM，而蓝色区域则是L2缓存，它们位于GPU芯片内部，紧邻SM，虽然不在SM内部，但物理上还是很近，因此速度也相当快（大约是L1的十分之一速度，但依然算快）。而在芯片外部，比如这是一块PCIe A800卡，GPU在这里，而DRAM实际上挨着芯片，它必须从芯片外走线连接。你可以看到芯片图上边缘的这些黄色连接器，它们是HBM接口，连接到实际GPU外部的DRAM芯片。你可以看到访问这些存储的时钟周期数差异：SM内部的内存快得多，大约20个时钟周期；而L2缓存或全局内存则需要200到300个时钟周期。10倍的速度差距会带来严重的性能问题。如果某段计算需要频繁访问全局内存，那很可能会导致SM上没有足够的工作可做，你完成了所有矩阵乘法，然后只能空闲等待，导致利用率低下。这将是贯穿始终的关键主题——思考内存实际上就是思考GPU工作方式的核心。在作业二中，你们将真正为GPU编写高性能代码，因此必须考虑GPU的实际执行模型。这稍微有些复杂，但并非无法理解。需要考虑三个粒度级别：块（blocks）、线程束（warps）和线程（threads），粒度从粗到细。块是线程的大组，每个块会被分配到一个SM上。可以这样想：每个SM是一个工人，是自治单元，一个块被指派给一个SM来处理。而在块内部包含大量线程，每个线程是需要完成的一个小任务。线程执行时又会按组进行，这个组就叫线程束（warp）。具体地说，你有一个块（线程的集合），然后从中取出一些线程，每次以连续的32个线程为一组来执行，这就叫一个线程束。看一下这个图，可以看到有一堆块，每个块被分配给不同的SM，而每个块内又有许多线程束，每个线程束又由大量线程组成，所有这些线程都在不同的数据上执行相同的指令。这就是GPU的执行模型。现在你可能觉得块、线程束、线程的概念有些神秘，但之后在设计CUDA内核时，它们对性能的影响会十分显著，希望大家先有个印象。除了逻辑上的执行模型，GPU还有一个逻辑上的内存模型。我现在不展示物理硬件，而是如何从编程角度思考GPU。寄存器是最快的，用于存储单个数字；本地内存；共享内存；全局内存。从左到右，内存层级越来越高，速度越来越慢。代码可以写入全局内存，也可以写入常量内存（虽然不太常用）。每个线程可以访问自己的寄存器和共享内存，但跨块的信息必须通过全局内存来传递。这一点非常重要。当你写一个执行某些计算的线程时，它通常操作一小部分相同的数据，所以你把这小部分数据加载到共享内存中，所有线程都很愿意访问共享内存，计算完成后就结束，这是一种理想的执行模型。而如果一个线程需要到处访问数据，那就必须用到很慢的全局内存。在讨论不同的GPU操作方式时，这一点会反复出现。希望我讲清楚了。这大致就是关于GPU的极高层次的四页幻灯片概述。如果有任何疑问，可以随时问我。接下来是一个旁支话题。去年我没有讲这个，因为当时关于TPU的资源还很少，但今年初我提到的那本很棒的GPU书（或网站）出版了，里面有很多细节。我和几位谷歌员工聊过，发现TPU在高层抽象上和GPU非常相似，所以我花一点时间介绍TPU。你可能永远不会直接操作TPU，但理解这些替代加速器在许多方面运作方式类似，这很重要。这是TPU的架构图。它有一个叫做Tensor Core的东西，你可以把它类比为SM或流式多处理器，每个都是一个原子单元，能操作数据。其中包含标量单元（基本上就是控制单元，可以执行类似CPU的任意操作）、向量单元（可以对向量进行逐元素操作，很适合做这类运算），还有一个非常大的、专用于矩阵乘法的部分，叫MXU。此外，它还有非常快的片上存储：向量存储和SMM，这些是Tensor Core内部的高速内存。然后还有高带宽内存（HBM）位于芯片外部。你看到它和SM的相似之处了吧？外部有慢速内存，内部有非常快的内存，还有专门做矩阵乘法的硬件。核心结构非常相似。不同之处在于，我将在下周的并行计算讲座中讨论这些加速器是如何互联的。另外，我注意到我没有谈论线程束等等，因为TPU在某种程度上很简单——它经过优化，基本上只做矩阵乘法。Tensor Core不像GPU那样试图做其他任何事情，因此架构更简单，但概念上做的事情是相同的。有人问：它叫Tensor吗？当然，在某些方面，它是为了处理内部张量而优化的。问题是，它是不是叫Tensor，因为它可以操作任意张量？它确实可以操作任意张量，比如索引操作。但MXU执行的操作是矩阵乘法，所以它总是像批量矩阵乘法一样处理张量。所以答案既是也不是，它操作张量，但总是执行矩阵乘法，而不是更复杂的张量操作。懂了。GPU之所以如此成功的一个原因是它很容易扩展。想要更多处理能力？加更多SM就行了，不用担心提升时钟频率带来的散热问题。编程上，CUDA有些吓人，但由于其编程模型，它实际上并不那么恐怖。它的工作原理是，在每个SM内，一个线程对一堆不同数据执行相同指令，这个概念上相对容易推理，尤其是当你操作矩阵并做简单操作时，正好符合这个简单模型。最后，这些线程非常轻量，可以随时暂停和启动。如果需要等待另一个线程，或者需要切换上下文，这些线程非常轻量，开销很小。这意味着GPU能在每个SM内获得高利用率。GPU当然是图形处理单元，在其早期大部分时间并未用于科学计算。但因为它可编程，研究人员想出了如何在早期的NVIDIA GPU上进行快速矩阵乘法。这是一篇早期论文，展示了如何利用纹理缓存等机制来实现矩阵乘法。也就是说，即使没有专门的矩阵乘法支持，研究人员也能找到办法。但现在，NVIDIA和其他公司已经认识到矩阵乘法是特殊的操作。如果你在做深度学习，大部分工作负载就是矩阵乘法，所以在某种程度上，矩阵乘法是受优待的操作。这张图显示了几代NVIDIA GPU的teraflops数值。橙色线是矩阵乘法的算力，蓝色线是非矩阵乘法的算力。可以看到从V100开始，他们加入了专门的Tensor Core来执行矩阵乘法，矩阵乘法性能与非矩阵乘法性能之间产生了巨大鸿沟。所以，如果你要设计任何神经网络架构，正如我在架构部分所说的那样，你必须让大部分工作负载成为矩阵乘法，因为这是GPU上比其他操作快数个数量级的运算。如果你构建一个不基于矩阵乘法的神经网络，那就会有大麻烦。最后，我想让你了解一些基本事实：矩阵乘法很快，但另一个需要记住的要点是GPU不同组件的相对扩展速度。这张非常好的图展示了GPU或整个LM训练栈不同组件的增长速度。蓝色线是GPU到主机的互联带宽（比如PCIe、NVLink等各种高级互连），它们在增长，但相对较慢（该图是相对于第一代互联的归一化带宽）。绿色线是全局内存速度，从GDDR到HBM2E，快了很多（对数坐标下快了100倍），但扩展速度仍不算快。而灰色线是计算扩展，即矩阵乘法的浮点运算次数，其增长速度惊人，快了100到1000倍。在早期，你的瓶颈可能是算力，你缺少足够的FLOP来做矩阵乘法。但现在，到了最右边的H100，这些GPU快得惊人，你的瓶颈很可能会变成内存，因为内存的增长速度跟不上。展望未来，这种趋势不会改变。DRAM很难再大幅扩展，而算力与内存之间的差距会越来越大。因此，如果你设计硬件高效的算法，就必须越来越多地考虑内存。这也是我们要不断强调的主题，是GPU中最重要的主题之一。好了，我刚刚抛出了很多GPU事实，尤其是如果你最近没接触过这些的话。总结一下：GPU是大规模并行处理系统，同一条指令被应用于大量不同线程。它们包含许多叫做SM的核心单元。计算和矩阵乘法的扩展速度极快，而且扩展速度快于内存，这是思考GPU时的一个重要特征。但也不是所有内存都慢，我们还有内存层级，有些内存非常快，有些则慢，因此如果能善用这个层级，就可以获得极高的性能。这些就是要记住的GPU关键事实，记住它们，你就能清晰地思考我接下来要讨论的性能问题。有疑问吗？没有的话，我们继续。现在你们都成了GPU专家，我们的目标就是让机器学习工作负载在GPU上跑得非常快。我从这张图表开始，我们的目标之一就是理解它到底表明了什么，这也算一个激励性的谜题。这张图显示的是不同尺寸的方阵乘法：x轴是方阵大小，y轴是每秒的运算次数，可以看作硬件利用率。矩阵越大，利用率越好，因为有更多工作可以抵消作业启动等开销。但图中出现了各种奇怪的现象：有四条不同的线，而且每条线都呈波浪形，看起来难以预测。我们正是要理解这些线上发生了什么。我保证到本节结束时，你们就能理解每一种现象，并可以说，“那张图完全正常，GPU就该这样。” 首先，图的整体形状有点像下图，学过系统硬件课程的同学应该记得这是屋顶线模型（Roofline model）。这个模型说明在吞吐量或利用率上会有两个区域：左侧斜线部分是内存受限区域（绿色），右侧平坦部分则是吞吐量受限区域。可以这样理解，右侧我们的计算单元得到了充分利用，矩阵乘法一直在运行；而左侧斜线部分，我们的计算能力受限于内存带宽，可用的FLOP/Byte比率不够高。我们希望避免处于左侧内存受限的区域，尽量处于右侧以获得计算单元的完全利用。所以目标就是那个。这张屋顶线模型大概长这样：有一段斜线，然后在最顶部有个平坦部分。这就是谜题的一部分。要避免不必要地访问慢速全局内存，说来简单，但需要一大套技巧。有很多事会让你陷入低效，其中第一个甚至不是内存瓶颈，我先提一下，然后讨论剩余五个核心的GPU性能考量。首先要讨论的是条件分支。如前所述，GPU的执行模型是SIMT（单指令多线程），一个线程束里的所有线程在同一时刻执行同一条指令（但作用于不同数据）。如果我写一段这样的代码：if (threadIdx < 4) 做A，else做X。在GPU上运行时，它会先在四个线程上执行A指令，同时暂停另外四个本应执行else部分的线程；然后这四个线程苏醒去执行X，之前那四个线程进入休眠；如此交替执行。为什么？因为同一线程束内不能同时执行A和X指令，必须所有线程统一执行相同指令。所以同一线程束内的条件分支会造成严重损失。这很明显，你不应该在大规模并行计算单元里放入条件分支。除掉这一点，其他技巧基本都和内存有关。第一个是低精度，这是一个大技巧，非常重要，应该始终使用。回到Build Ali的那张图，其实有个障眼法：表面上数字一直在增长，但实际上驱动GPU进步的一个重要因素是数值表示。从FP32到FP16再到INT8等，光是降低精度就能带来数量级的增益。我来解释为什么：如果计算和权重所用的位数更少，那么需要移动的位数也大大减少。即使还是从全局内存中访问，数据量也小得多，瓶颈就减轻了。举个简单的例子：考虑一个简单的逐元素操作，“y = max(0, x)”在大小为n的向量上执行，使用float32。需要多少次内存访问？要读取x，然后写入结果，全都是float32，所以总共8字节。操作数呢？做一次比较x<0，所以是一次比较操作，相当于一次FLOP。所以每FLOP对应8字节。如果用float16，FLOP强度未变，但内存访问变成4字节。相当于免费获得了一倍的内存带宽，只要你能承受float16。这是很多设计的关键部分。作业的一部分就是让你尝试各种混合精度或低精度训练。关键之处在于，并不是网络和训练算法的所有部分都可以降低精度。举例来说，矩阵乘法采用混合精度：输入用16位（低精度），但乘法在32位全精度下进行，因为中间累加部分和需要高精度，然后Tensor Core返回FP32结果，最后你可以再降回16位。所以输入是16位，但累加可能需要32位。有些操作可以用16位存储，有些需要更高精度（保持FP32或FP16），还有些操作需要更大动态范围（如激活函数，如果动态范围不够会导致溢出或归零），这时可能要用bfloat16。需要精心设计，以确保模型在低精度训练下保持稳定。如果能做到，那就很棒，因为瓶颈的吞吐量大致翻倍（从32位到16位）。记住，这些是瓶颈。另一个技巧是算子融合，很多人提到“我写一个CUDA内核”时想到的就是这个。它既直观又有趣。一个很好的心理模型来自Horace He的工厂图：想象工厂是计算部分，它接收小方块原料输出小三角产品，如果计算单元变多了，但内存到计算的传送带带宽有限，你多出来的工厂依然用不上，仍然受限于传输速度。这就是内存瓶颈。但不那么明显的一种产生大量开销的方式，正是左图的计算模式。图中左侧是内存，右侧是计算单元。我先把方块从内存搬到计算单元，做一些操作变成三角形，再把三角形送回内存，之后又发现需要这些三角形，再搬回计算单元，变成圆形，如此反复。如果你幼稚地在GPU上逐个执行操作，并把中间结果直接写回全局内存，就会得到这样的效果。算一下数据往返的次数，这非常糟糕，产生了巨大的内存开销。如果我们意识到这些计算之间没有依赖，完全可以连续地执行方块→三角形→圆形→矩形，最后只把矩形写回，让数据一直留在计算单元内。这就是右图的模式，也是融合内核的心理模型：一系列操作会依次作用于同一块数据，与其每次写回内存，不如尽可能在计算单元内完成所有操作，直到最终才写回。这就是内核融合。举个非常简单的例子：假设我写了一个神经网络模块，计算sin(x)，并返回sin²(x)和cos²(x)。这样简单的代码，在PyTorch里的计算图会生成一堆CUDA内核：一个计算sin(x)，一个计算cos(x)，一个计算sin²，一个计算cos²，最后还有相加的。这就导致大量的来回传输，非常像左图。但如果你聪明一点，自己写CUDA内核或用torch.compile，就会发现这五个操作不需要用到多少内存，可以融合成一个操作，在GPU上用一个线程全部完成，而不必将数据频繁写回全局内存。这种简单的融合编译器可以自动完成，所以我提到torch.compile，你如果还没用，强烈建议到处使用它。作业中也会用到torch.compile，真的很方便。好了，我已经讲了精度和融合，有疑问吗？没问题我们接着讲重计算（recomputation）。重计算的核心思想是：花更多计算来避免内存访问。回想一下反向传播的原始讲解——这是CS21的例子——我们将底部的输入向上传播激活值（图中黄色的值），然后反向计算雅可比矩阵（绿色的边值），再将雅可比与激活值相乘来传播梯度。但仔细想想，那些黄色激活值在前向之后必须存下来，然后反向时再从全局内存中取出来送入计算单元。这么一来一回可能产生大量内存读写。实际上，我们可以通过重计算来加速。举个简单的例子：我把三个sigmoid叠起来，前向图就是三个sigmoid依次作用。计算图会计算sigmoid，并存储中间激活值s1和s2。前向过程需要读一次x，写三次s1、s2和f。反向时，需要从内存读取s1、s2以及梯度信号，然后得到dx。总共8次内存读写（读x，写s1、s2、f，再读s1、s2和梯度，最后写dx），算术强度极低。重计算的想法是：我不存储那些激活值，而是在反向传播时动态重新计算。于是，新的前向我不存s1、s2，只读x，写loss；反向时我读入梯度信号和x（两次读），然后在小SM和本地内存中实时计算每个sigmoid值并用于反向图，即重新计算s1、s2和输出，无需全局内存读取，最后只写dx。对比一下，内存访问降到了原来的五分之八。代价是多算了三次sigmoid，但如果你本来就因内存受限而处于空闲状态，这个交易非常划算——用冗余的计算换取了紧缺的带宽。这其实跟梯度检查点、重计算激活以节省显存是相同的技巧，但目标不同，这里是为了执行速度。接下来这个技巧非常有趣，我也是深入研究GPU和DRAM硬件模型后才知道。全局内存（即DRAM）其实非常慢，为了加快访问，硬件层面做了一些优化，其中之一就是“突发模式”（burst mode）：当你去读一个内存地址时，你得到的不仅仅是那个值，而是那一整块数据（比如四个字节）。比如你只想读内存块的第一个值，内存不仅给你那个0，还会把0、1、2、3四个值一起返回。每一个地址空间被划分成所谓的突发段，你得到的是整个突发段。这听起来很奇怪：为什么内存要白白多给你三个字节？有个很有趣的硬件原因：在内存中寻址时，为了把信号送出去，需要把这些字节移到放大器，这步最慢。一旦完成了这一步，后续的字节传输几乎零代价，所以就诞生了突发段，以此掩盖数据移动到放大器的高昂成本。不管怎样，这意味着如果我们内存访问模式得当，就可以显著加速。例如我们要读取一整块数据，如果随机访问，查询次数大约等于所需值的个数；但如果先查第一个值，一次获得整个突发段，然后查第四个值，获得第二个突发段，那么吞吐量可能提升四倍。如果我们能聪明地只访问每个突发段中需要的位，就可以大大提高效率。这叫做“内存合并”（memory coalescing）。如果一个线程束中的所有线程访问都落在同一个突发段内，那么智能硬件和编程模型就会把这些查询合并：不用分别请求0、1、2、3，而是直接请求0，然后从突发模式DRAM中一次读出0、1、2、3。记住，一个线程束有32个连续编号的线程，所以它们的访问是一起发生的。当这些线程束读取突发段时，就能通过优化一次获得四个字节，而不是一个接一个地获取。这将使内存带宽变为四倍。这些看似简单的细节实际上非常重要。想象一下，如果你要自己用CUDA实现神经网络，矩阵乘法是核心。你需要选择是按行遍历还是按列遍历矩阵（即每个线程负责一行向下走，或者负责一列向右走）。结果发现，左边这种按列遍历（每个线程走一列）会很慢，因为内存访问无法合并；而右边这种按行遍历（每个线程走一行）就能合并。想一下为什么会这样？在右侧图中，如果线程要从左到右读取，即第一个时间步，各个线程分别加载第一列的第一个元素、第二列的第一个元素……这些元素位于不同的突发段，无法合并，必须分别读取整个内存块。相反，如果按列方向（每个线程固定负责一列，依次向下读取），那么所有线程在同一个时间步会读取同一行内的连续地址，正好落在一个突发段内，一次内存读操作就能拿到所有数据。这是一个非常底层的优化，但至关重要——如果你的内存遍历顺序错了，实际内存访问会变得非常慢。最后一个大技巧是“分块”（tiling）。分块的思想是，把内存访问分组以减少全局内存访问。为了解释清楚，我通过矩阵乘法的例子来说明为什么朴素的算法会有很多问题，然后再给出分块版本，看看如何减少全局内存读取。从一个最简单的矩阵乘法算法开始：要计算矩阵乘，需要遍历M的行和N的列，做内积并存入P的对应位置。我写明了每个线程所对应的输出元素以及它们访问各个元素时的顺序。注意，这里内存访问不是合并的，行矩阵M的访问不是连续的；而且有大量重复的内存访问——例如M[0,0]被多个线程读取，N[1,0]也是。这些值从全局内存中被反复读取，非常慢。那么，能否避免过多的全局内存读写呢？我希望达到的理想结果是：花一段时间把数据从全局内存搬到快速共享内存，然后在共享内存中进行大量计算，最后再把结果写回。这样全局内存的访问次数就降到了最低。那么如何在矩阵乘法中实现？办法是将M和N矩阵切分成小块（tiles），比如2×2的块。假设共享内存足以容纳这些子矩阵，我们就有了一种非常简单的算法：首先加载M的左上角tile（M[0,0]）和N对应的tile（N[0,0]）到共享内存，然后计算部分积并累加到P的相应位置。接着加载下一块，重复。这样一来，我大幅减少了全局内存访问：一次加载尽可能多的数据到共享内存，完成子矩阵所能完成的所有计算，然后再处理下一个tile。另外，由于我是按tile加载，可以自由选择遍历子矩阵的顺序（如列主序或行主序），从而对所有存储器访问进行合并。所以分块带来了多方面的好处。做一些简单的分块数学：假设方阵大小为n，tile大小为T。如果是非分块的矩阵乘法，每个元素都要从全局内存读取n次。而采用分块，每个输入元素从全局内存读取n/T次，但在tile内部会读取T次。虽然总读取次数不能减少（必须读每个矩阵元素），但我们可以把大部分读取转移到快速的共享内存中：有T次内存读在共享内存进行，只有n/T次从全局内存读。如果共享内存够大能存大tile，这相当于把全局内存读取量减少了T倍。所以分块非常强大。但分块也带来很多复杂性，这是导致GPU矩阵乘法性能变得诡异的一大根源。一个问题与离散化有关。假设tile大小为128，如果全矩阵尺寸是256，一切很完美，2×2刚好覆盖。如果列维度为256，但尺寸不是刚好整除，比如有257列，就会有6个tile，但最右侧的两个tile非常稀疏，里面没多少工作。问题在于每个tile会被分配给SM，即一个块，那些右侧的稀疏tile所在的SM将处于半闲置状态。如果是计算受限的情况，你会希望负载更均匀地分配到各SM。因此必须优化tile大小以尽量避免这种情况。但实际上设计tile大小很复杂：既要保证内存访问合并，又不能超过共享内存大小，还要尽量整除矩阵维度或接近整除，避免最后出现利用率低下的SM。有人问，能否重叠内存读取和计算？是的，GPU自然会尝试利用可用带宽，只要共享内存有空闲，就可以预先加载。但问题是，当SM被有效利用时，共享内存通常是瓶颈资源，可能没有地方做预取。另一个与burst段相关的复杂问题：假设矩阵布局正好让burst段与tile对齐，读取一个tile只需四次burst段读取。如果矩阵尺寸只多出一个元素，导致tile的第二行跨在两个burst段上，那么第二行就需要两次读取，依此类推，内存访问量几乎翻倍，而这只因为末尾多了一个元素使对齐破坏。如果tile尺寸或矩阵大小不是burst段的倍数，就很容易出现这种行列不对齐的情况，大幅增加内存访问量。为避免这点，需要进行padding，使尺寸与burst段倍数匹配。这虽然很底层，但如果想榨干矩阵乘法的全部性能，就必须考虑这些，否则就会吃亏。当然，像torch.compile和所有CUDA优化的矩阵乘法，做的正是这些。所有这些复杂性最终导致类似这样的现象：我最喜欢的一条Karpathy的推文说，nanoGPT中最显著的优化就是把词表大小从50257增加到5304（最近的64的倍数），从而获得高得多的占用率，并且要注意2的幂次。就这样增加了25%的速度，仅仅通过调整词表维度。这怎么发生的？说到这里，我们终于可以回到开头的谜题。我带着大家过了一遍GPU的性能细节，就是希望你能完全理解，那张矩阵乘法性能图不再神秘。第一部分很简单，就是计算强度，这正是屋顶线模型。在约1536尺度之前，矩阵乘法工作量不够，加载矩阵和基础IO成为瓶颈，所以吞吐量急剧下降。过了这个点，理论上画个上包络线，就能达到峰值性能，即让所有计算单元饱和，获得极佳性能。但如果不小心搞砸了矩阵尺寸，就会落到这些奇怪的谷区。我们来分析一下为什么会有这些不同的表现。首先，第一条线是tiling对齐问题。现在我把各条线按能被哪些数整除进行了着色：如果能被32整除，就是紫色点，表现很好；能被16整除，其实也在上面（两种颜色重合）；能被8整除是绿色；能被2整除是橙色；不能被任何大于1的数整除（即奇数）是红色，表现极差。如果选了质数大小的维度，肯定得不到好性能。很大一部分原因是，当整除情况变差（尤其gdc=2或1），就无法以对齐突发段的方式读取tile，导致严重问题。另外，即使在同一整除类别内，还有另一种谜团。放大橙色线，从1792增加到1794（假设），性能会急剧下降。为什么？让我们用数据说话：假设使用256×128的tile（很自然的尺寸，GPU的矩阵乘法单元天然适合128左右的矩阵）。1792/256=7，1792/128=14，共98个tile。如果增加一维，大小变成1793（或1794），就要向上取整，tile数变为8×15=120个，增加了不少，且一部分tile利用率低。更糟糕的是，A100有108个SM。98个tile时，可以一次性全分派出去，所有SM都在忙碌，利用率高；但120个tile时，超过了SM数量，那么108个SM执行完第一波，剩余的12个tile必须等有SM空闲后再执行，这些尾量利用率极低，导致整体吞吐骤降。这叫做“波浪量化”（wave quantization）效应。理想情况下，tile数量应远大于SM数，或避免刚刚超过SM数一点点而导致这种浪费。好了，我知道这些都是底层细节，但在很多课上我都说过，语言模型和深度学习成败在于细节。正是对这些细节的关注，才让人们能把模型扩展到非常大的规模并保持高性能。因此即使你不打算做系统工程，也值得了解。总结一下，关键的理念是什么？第一，必须减少内存访问。方法很多：合并访问，以免费复用读取；算子融合，避免不必要的读写；分块，将数据移到共享内存；以及用计算或精度来换内存，即重计算和量化。你有很多手段来榨取性能，关键在于时刻留意内存对GPU性能的影响。这就是核心。有问题吗？好。现在，我将把所有这些技巧融会贯通，之前那些零散的GPU知识会组成一个标准的性能优化工具箱，而FlashAttention和FlashAttention-2将示范如何把它们组合起来，构建现代高性能Transformer的基石之一。大家都知道，FlashAttention极大地加速了注意力计算，这通过一些CUDA内核魔法实现，但可能并不完全了解细节。论文里说，通过对未优化的PyTorch Transformer实现进行内核融合等操作，能获得显著加速。原文提到：“我们应用了已有的技术——tiling和重计算，以克服精确注意力计算中次二次的HBM访问这一技术挑战。”注意，计算上注意力一般无法降到次二次，但他们做到了对全局内存（HBM）的访问是次二次的。这才是关键：如果内存是瓶颈，你希望把它变得非二次方，至少让二次方的代价由计算而非内存承担。简单回顾一下，你们已经多次实现过注意力：它由K、Q、V三个矩阵乘法加一个softmax组成。矩阵乘法很简单，可以用tiling解决，我之前展示过例子。注意力特别之处在于有softmax，这才是难点。一旦能处理softmax，所有我讲的矩阵乘法技巧就都能应用上了。矩阵乘法部分就是标准的分块矩阵乘法，在FlashAttention的图一中，K和Q切分成小块，复制到SRAM，相乘，然后累加、写回HBM，其间做softmax再与V相乘。这些都很直观。但现在需要处理softmax。Softmax的问题在于它是一个全局操作，在注意力中是按行执行的，必须对整个行求和归一化。这很麻烦，因为理想状态下我们希望所有运算都在tile内完成，不写回大矩阵。因此需要一个能在每个tile内在线计算的softmax，即“在线softmax”（online softmax）。如果你有一个值流，正常的批量softmax会取所有x1..xn，指数化、求和、做除法。而在线softmax（Milakov和Gimelshein 2018年提出）通过保持当前最大值m_j和校正项d_j，可以流式更新。当新数据到达时，如果发现新的最大值就调整校正因子，然后累加当前项。最终，d_n就是归一化项的和，然后可以直接得到归一化的y_i。关键是可以在线计算，不需要提前知道所有x1..xn。这样就可以按tile逐个计算softmax：在每个tile内运行这个算法，得到该tile的部分softmax和所需跟踪量。这样我们永远不需要物化完整的n²矩阵。这就是核心。把这一点融入整体，就得到了FlashAttention的前向过程。如果你去看FlashAttention-2的论文（我们作业会要求你实现），会看到完全相同的思路：首先进行KQ的tiled矩阵乘法，在线更新指数和，并逐步用最大值校正，然后逐个tile累加，最后再与V的tile相乘，得到最终的softmax输出。有人问，是不是得在所有tile算完才能输出？是的，必须遍历完所有n²个tile才能得到最终结果，但遍历一次后，就已经有了所有必要的归一化项，因此不需要再次重复计算。至于反向传播，我没有展开讲，但基本思想是可以通过逐tile重计算来避免存储n²大小的中间激活。前向时我们已经靠tile避免了存储n²矩阵，反向时如果存储激活值又会变成n²大小，所以要动态重新计算。这是FlashAttention实现高效反向的另一个关键技巧。其他部分就是标准的梯度计算逐tile进行。好了，到这里我们该结束了。希望大家看到，我之前讲的分块、合并、重计算等所有部件，是如何组合在一起，诞生了FlashAttention，以及让Transformer跑得更快的那些酷炫东西。最后总结一下：硬件是驱动当今语言模型发展的核心力量。要真正发挥硬件的威力，你必须理解底层细节。当前GPU扩展的趋势图清楚地激励我们去思考内存移动。内存移动是所有问题的瓶颈，所以不要只想着减少FLOP（虽然也重要），更要思考如何让内存移动更高效。最后，如果要进行一定量的计算，优化方法就是优化数据移动，尽可能避免从高带宽内存（全局内存）移动数据，把所有东西留在极快的共享内存中。这就是FlashAttention等获得高性能的根本。谢谢大家。